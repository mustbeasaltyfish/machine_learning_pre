## 幻灯片内容大纲（任务一 SimCLR + 任务二 VAE）
1. 标题页：实验主题、课程、组员、时间
2. 目录页：任务一（SimCLR）、任务二（VAE）、对比实验、结论
3. 背景：自监督 vs 监督学习的动机（标注成本、泛化能力）
4. 任务一目标：SimCLR 要解决的问题与核心思路（对比学习）
5. SimCLR 流程概览：数据增强 → 编码器 → 投影头 → NT-Xent
6. 数据增强细节：强增强对比示例（裁剪/颜色抖动/模糊）
7. 模型结构：编码器 + MLP 投影头（结构图）
8. 损失函数：NT-Xent 直观解释（正样本拉近、负样本推远）
9. 训练曲线：SimCLR loss 曲线（动态折线图）
10. 表征可视化：t-SNE/UMAP 散点图（按 CIFAR 标签着色）
11. 任务二目标：VAE 的生成与重构能力
12. VAE 流程概览：编码 → 重参数化 → 解码 → Loss
13. VAE 损失：重构损失 + KL 散度（动态图表）
14. 重构对比：原图 vs 重构图（网格对比）
15. 生成效果：随机采样生成（可选）
16. 对比与讨论：SimCLR 表征质量 vs VAE 重构质量
17. 结论与下一步：可改进点（更强增强、更深编码器、更大 latent）